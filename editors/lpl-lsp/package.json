{
  "name": "lpl-lsp",
  "version": "0.1.0",
  "description": "Language Server Protocol implementation for the LPL programming language",
  "author": "London Sheard",
  "license": "Apache-2.0",
  "main": "out/server.js",
  "bin": {
    "lpl-lsp": "out/server.js"
  },
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --outfile=out/server.js",
    "watch": "esbuild src/server.ts --bundle --platform=node --outfile=out/server.js --watch"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.0.0"
  }
}
