include <files>;
include <console>;

int main() {
    File f = File("README.md");
    string line = f.readByLine();
    while (line != "") {
        Console.println(line);
        line = f.readByLine();
    }
    f.close();
    return 0;
}