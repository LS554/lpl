include <console.lph>;

void main() {
    const int x = 42;
    Console.printInt(x);

    // This should cause a compile error if uncommented:
    // x = 99;

    const int y = x + 8;
    Console.printInt(y);
}
